{
  "command": "python3 -u app/app.py",
  "build_path": "/",
  "stack": "python_3_10",
  "port": 5000,
  "memory": 512,
  "env_variables": {},
  "scripts": {
    "build": "pip install -r requirements.txt --break-system-packages"
  }
}
